export const DEFAULT_CONFIG = {
  corepath : 'bitcoind',
  clipath  : 'bitcoin-cli',
  // confpath : `${process.cwd()}/bitcoin.conf`,
  datapath : `${process.cwd()}/coredata`,
  network  : 'regtest',
  params   : []
}
